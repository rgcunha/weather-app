import React from 'react'
import FilterableCities from '../../components/filterable-cities'

const SearchView = props => (
  <div className="search">
    <h1>Search</h1>
    <FilterableCities />
  </div>
)

export default SearchView
